---
audience: ai-deep
block: telegram-bot
source: auto-gen
kind: telegram
title: "Telegram Events — telegram-bot"
---
# ACCEPTED
<a id="tg-accepted"></a>

Telegram event `ACCEPTED`.

Template: `src/server/telegram/templates/accepted.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("ACCEPTED"`.

---

# AFFILIATE_DAILY_SUMMARY
<a id="tg-affiliate-daily-summary"></a>

Telegram event `AFFILIATE_DAILY_SUMMARY`.

Template: `src/server/telegram/templates/affiliate-daily-summary.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("AFFILIATE_DAILY_SUMMARY"`.

---

# AFFILIATE_FTD
<a id="tg-affiliate-ftd"></a>

Telegram event `AFFILIATE_FTD`.

Template: `src/server/telegram/templates/affiliate-ftd.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("AFFILIATE_FTD"`.

---

# ALERT_TRIGGERED
<a id="tg-alert-triggered"></a>

Telegram event `ALERT_TRIGGERED`.

Template: `src/server/telegram/templates/alert-triggered.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("ALERT_TRIGGERED"`.

---

# ANOMALY_DETECTED
<a id="tg-anomaly-detected"></a>

Telegram event `ANOMALY_DETECTED`.

Template: `src/server/telegram/templates/anomaly-detected.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("ANOMALY_DETECTED"`.

---

# AUTOLOGIN_DOWN
<a id="tg-autologin-down"></a>

Telegram event `AUTOLOGIN_DOWN`.

Template: `src/server/telegram/templates/autologin-down.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("AUTOLOGIN_DOWN"`.

---

# AUTOLOGIN_SLA_BREACHED
<a id="tg-autologin-sla-breached"></a>

Telegram event `AUTOLOGIN_SLA_BREACHED`.

Template: `src/server/telegram/templates/autologin-sla-breached.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("AUTOLOGIN_SLA_BREACHED"`.

---

# BROKER_CONFIG_CHANGED
<a id="tg-broker-config-changed"></a>

Telegram event `BROKER_CONFIG_CHANGED`.

Template: `src/server/telegram/templates/broker-config-changed.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("BROKER_CONFIG_CHANGED"`.

---

# BROKER_DOWN
<a id="tg-broker-down"></a>

Telegram event `BROKER_DOWN`.

Template: `src/server/telegram/templates/broker-down.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("BROKER_DOWN"`.

---

# BROKER_RECOVERED
<a id="tg-broker-recovered"></a>

Telegram event `BROKER_RECOVERED`.

Template: `src/server/telegram/templates/broker-recovered.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("BROKER_RECOVERED"`.

---

# BROKER_REJECTION_STREAK_PAUSED
<a id="tg-broker-rejection-streak-paused"></a>

Telegram event `BROKER_REJECTION_STREAK_PAUSED`.

Template: `src/server/telegram/templates/broker-rejection-streak-paused.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("BROKER_REJECTION_STREAK_PAUSED"`.

---

# CAP_REACHED
<a id="tg-cap-reached"></a>

Telegram event `CAP_REACHED`.

Template: `src/server/telegram/templates/cap-reached.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("CAP_REACHED"`.

---

# DAILY_SUMMARY
<a id="tg-daily-summary"></a>

Telegram event `DAILY_SUMMARY`.

Template: `src/server/telegram/templates/daily-summary.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("DAILY_SUMMARY"`.

---

# DECLINED
<a id="tg-declined"></a>

Telegram event `DECLINED`.

Template: `src/server/telegram/templates/declined.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("DECLINED"`.

---

# FAILED
<a id="tg-failed"></a>

Telegram event `FAILED`.

Template: `src/server/telegram/templates/failed.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("FAILED"`.

---

# FRAUD_HIT
<a id="tg-fraud-hit"></a>

Telegram event `FRAUD_HIT`.

Template: `src/server/telegram/templates/fraud-hit.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("FRAUD_HIT"`.

---

# FRAUD_POLICY_CHANGED
<a id="tg-fraud-policy-changed"></a>

Telegram event `FRAUD_POLICY_CHANGED`.

Template: `src/server/telegram/templates/fraud-policy-changed.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("FRAUD_POLICY_CHANGED"`.

---

# FTD
<a id="tg-ftd"></a>

Telegram event `FTD`.

Template: `src/server/telegram/templates/ftd.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("FTD"`.

---

# INVOICE_FAILED
<a id="tg-invoice-failed"></a>

Telegram event `INVOICE_FAILED`.

Template: `src/server/telegram/templates/invoice-failed.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("INVOICE_FAILED"`.

---

# INVOICE_PAID
<a id="tg-invoice-paid"></a>

Telegram event `INVOICE_PAID`.

Template: `src/server/telegram/templates/invoice-paid.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("INVOICE_PAID"`.

---

# MANUAL_REVIEW_QUEUED
<a id="tg-manual-review-queued"></a>

Telegram event `MANUAL_REVIEW_QUEUED`.

Template: `src/server/telegram/templates/manual-review-queued.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("MANUAL_REVIEW_QUEUED"`.

---

# NEW_LEAD
<a id="tg-new-lead"></a>

Telegram event `NEW_LEAD`.

Template: `src/server/telegram/templates/new-lead.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("NEW_LEAD"`.

---

# PENDING_HOLD_RELEASED
<a id="tg-pending-hold-released"></a>

Telegram event `PENDING_HOLD_RELEASED`.

Template: `src/server/telegram/templates/pending-hold-released.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("PENDING_HOLD_RELEASED"`.

---

# PENDING_HOLD_START
<a id="tg-pending-hold-start"></a>

Telegram event `PENDING_HOLD_START`.

Template: `src/server/telegram/templates/pending-hold-start.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("PENDING_HOLD_START"`.

---

# PROXY_POOL_DEGRADED
<a id="tg-proxy-pool-degraded"></a>

Telegram event `PROXY_POOL_DEGRADED`.

Template: `src/server/telegram/templates/proxy-pool-degraded.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("PROXY_POOL_DEGRADED"`.

---

# PUSHED
<a id="tg-pushed"></a>

Telegram event `PUSHED`.

Template: `src/server/telegram/templates/pushed.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("PUSHED"`.

---

# SCHEDULED_CHANGE_APPLIED
<a id="tg-scheduled-change-applied"></a>

Telegram event `SCHEDULED_CHANGE_APPLIED`.

Template: `src/server/telegram/templates/scheduled-change-applied.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("SCHEDULED_CHANGE_APPLIED"`.

---

# SCHEDULED_CHANGE_FAILED
<a id="tg-scheduled-change-failed"></a>

Telegram event `SCHEDULED_CHANGE_FAILED`.

Template: `src/server/telegram/templates/scheduled-change-failed.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("SCHEDULED_CHANGE_FAILED"`.

---

# SHAVE_SUSPECTED
<a id="tg-shave-suspected"></a>

Telegram event `SHAVE_SUSPECTED`.

Template: `src/server/telegram/templates/shave-suspected.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("SHAVE_SUSPECTED"`.

---

# STATUS_MAPPING_BACKFILL_PROGRESS
<a id="tg-status-mapping-backfill-progress"></a>

Telegram event `STATUS_MAPPING_BACKFILL_PROGRESS`.

Template: `src/server/telegram/templates/status-mapping-backfill-progress.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("STATUS_MAPPING_BACKFILL_PROGRESS"`.

---

# SUBSCRIPTION_CANCELED
<a id="tg-subscription-canceled"></a>

Telegram event `SUBSCRIPTION_CANCELED`.

Template: `src/server/telegram/templates/subscription-canceled.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("SUBSCRIPTION_CANCELED"`.

---

# SUBSCRIPTION_CREATED
<a id="tg-subscription-created"></a>

Telegram event `SUBSCRIPTION_CREATED`.

Template: `src/server/telegram/templates/subscription-created.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("SUBSCRIPTION_CREATED"`.

---

# SUBSCRIPTION_RENEWED
<a id="tg-subscription-renewed"></a>

Telegram event `SUBSCRIPTION_RENEWED`.

Template: `src/server/telegram/templates/subscription-renewed.ts`.

Defined in `src/server/telegram/event-catalog.ts`. Emit site: grep the codebase for `emitTelegramEvent("SUBSCRIPTION_RENEWED"`.

